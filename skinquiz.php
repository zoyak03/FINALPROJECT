var quiz = new Date().getHours();
if (quiz = ) {
  greeting = "Good morning";
} else if (quiz = 20) {
  greeting = "Good day";
} else {
  greeting = "Good evening";
  // do whatever you want with the checked radio
  alert(radios[i].value);

  // only one radio can be logically checked, don't check the rest
  break;
 }
}
